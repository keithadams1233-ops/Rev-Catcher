import { PhaseStub } from "@/components/phase-stub";

export default function RanksPage() {
  return (
    <PhaseStub
      title="Ranks"
      description="The normalized leaderboard (minimum eligible transactions enforced) and team goal progress land here in Phase 7 (Challenge Engine)."
      accent="rewards"
    />
  );
}
