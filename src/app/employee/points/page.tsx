import { PhaseStub } from "@/components/phase-stub";

export default function PointsPage() {
  return (
    <PhaseStub
      title="Points"
      description="The points wallet, backed by the immutable point ledger, lands here in Phase 8 (Gamification Engine)."
      accent="rewards"
    />
  );
}
