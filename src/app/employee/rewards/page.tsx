import { PhaseStub } from "@/components/phase-stub";

export default function RewardsPage() {
  return (
    <PhaseStub
      title="Rewards"
      description="The reward catalog and redemption flow land here in Phase 8 (Gamification Engine)."
      accent="rewards"
    />
  );
}
