import { PhaseStub } from "@/components/phase-stub";

export default function ManagerHomePage() {
  return (
    <PhaseStub
      title="Manager Home"
      description="Monthly revenue opportunity, top revenue leaks, active challenges, and reward ROI land here in Phase 2 (Manager UI)."
      accent="manager"
    />
  );
}
