import { PhaseStub } from "@/components/phase-stub";

export default function SettingsPage() {
  return (
    <PhaseStub
      title="Settings"
      description="Data Sources → Upload POS CSV, column mapping, category/margin configuration, and reward catalog management land here in Phase 5 (CSV Import) and Phase 2."
      accent="manager"
    />
  );
}
